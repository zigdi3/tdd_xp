describe("Autentication", () => {
  it("should receive JWT token when authenticate with valid credentials", () => {
    const x = 2;
    const y = 4;

    const sum = x + y;

    expect(sum).toBe(6);
  });
});
