describe("Authentication", () => {
  it("Should receive a JWT token when insert valid credentials", () => {
    const x = 4;
    const y = 2;
    const sum = x + y;

    expect(sum).toBe(6);
  });
});
